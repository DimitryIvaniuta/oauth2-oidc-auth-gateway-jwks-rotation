# oauth2-oidc-auth-gateway-jwks-rotation
Java, Spring Boot Auth Gateway: JWT validation with Redis-cached JWKS + seamless key rotation, claim→role mapping, Postgres/Flyway audit log, Kafka auth events, full integration tests + Postman
